---
title: "系统项目框架整理"
pubDatetime: 2025-05-12T09:31:13+08:00
draft: false
featured: false
tags:
  - python
  - fastapi
  - 后端
  - 项目架构
description: "系统项目框架整理，包括 database/api、storage、用户接口、报告接口等模块的功能说明"
---

这篇主要来让自己理一理系统项目的框架

## 1、database/api 和 storage

### 1.1 storage

其中基于 **storage** 下创建许多函数用于后面数据处理的，数据储存的函数，首先是定义一初始储存路径。其中包括：

1. 账号加载，读取账号文件看账号中有没有对于账号文件存在，没有返回空有就返回对应 json。
2. 保存账号，即是比如在注册 api 中引入保存账号信息功能，把账号信息写入 accounts.json 中。
3. 保存用户头像，将用户头像的路径写入，并且返回用户名。
4. 保存用户信息，用用户 id 定位用户储存文件，之后通过 dict 格式去更新用户信息。
5. 加载用户信息，指定输入为用户 id 定位，输出为可选的 dict 格式文件。其中错误返回有三种：用户不存在，格式错误和加载用户时出错。
6. 保存图片，将图片数据保存到本地主要是保存路径为主，并返回一个唯一的图片 ID。
7. 保存报告，生成报告时间，以及统一报告命名格式，之后写入储存。返回报告 id。
8. 加载报告，协调后端和数据库，从数据库中提出对应报告，随后将报告返回给前端。
9. 返回用户报告，用于用户报告历史展示界面，通过去辨别用户是患者还是医生。在报告信息中的关键信息中提出符合患者 id 或者是医生 id 的对应报告。
10. 报告评论加载，输入是报告 ID，输出对应评论列表。
11. 保存报告结果图，将报告结果照片的 id，append 在报告的后面。方便后面读取对应照片。
12. 获取报告图片，输入是图片 ID，输出对应图片流信息。
13. 更新报告状态如果算法端接口返回结果了，生成对应 diagnosis 了，那么我就会更改报告状态为生成。
14. 删除报告，输入是报告 ID，输出对 bool 判断是否删除成功。
15. 保存医生详细信息，输入是医生 ID。
16. 加载展示医生详细信息，输入是医生 ID，输出对应信息字典。

### 1.2 api

1. 判断是否为医生，返回 bool 类型值。
2. 添加报告，输入是对应报告的患者和医生，通过上面的报告储存，添加报告到数据库中。
3. 获取对应报告，用的上面的函数。
4. 查找信息。
5. 删除报告。
6. 更新报告内容，通过上面的报告储存，添加报告到数据库中。
7. 更新头像。
8. 更新照片。
9. 添加个人详细信息。
10. 查找指定账户。
11. 储存账户。

### 二、api/common

其次看到 api 下的 common 为通用接口，写这两个接口的目的是为了方便其他接口调用

这两个接口 `/api/image` 和 `/api/image/get` 是一个图片上传和获取的功能组合，通常用于用户上传图像数据并在之后查看或下载图像。下面逐一介绍：

**接口一：image**

功能：上传图片返回图片 id

```
接收表单文件：使用 form = await request.form() 读取前端上传的表单数据，其中 file = form.get("file") 提取了上传的文件对象。

鉴权：从请求头中读取 token：

token = request.headers.get("token", default=None)
如果没有提供 token，返回错误代码 34 和提示"权限不足"。

用户认证：通过 JWT 解析函数 resolveAccountJwt(token) 提取出当前用户账号。

上传图片：调用 uploadImage(file.filename, await file.read()) 上传图片，并返回一个 image_id。

返回图片 ID：用 ImageResponse(image=image_id) 形式返回，方便前端后续通过该 ID 下载。
```

**接口二：image/get**

功能：通过图片 id 获取图片数据流

```
接收参数：request 是继承自 TokenRequest 的 GetImageRequest，里面包含 token 和图片的 id。

鉴权与验证：

使用 resolveAccountJwt(request.token) 验证身份并提取用户信息。

加载图片数据：通过 load_image(request.id) 获取图像数据。

判断是否为空：

如果找不到对应 ID 的图像数据，返回一个默认图片 default.png。

返回图片流：

成功获取到图像数据后，使用 StreamingResponse([image_data]) 返回字节流（适用于浏览器显示或下载）。
```

## 2、api/user

### 2.1 login

创建几个类的登录请求，包含里面的用户名，密码以及类型，生成 token 的类。

其中包含两个接口：一个是登录接口，一个是注册接口

登录：首先是载入账号信息，之后再账号信息中查找用户名字。之后验证对应账号密码是否对的上。可以返回 JWT 令牌。

这边引入一个 JWT：

```python
from jwt import encode, decode
import datetime


def makeAccountJwt(account: str) -> str:
    secret = "this_is_the_secret"
    payload = {
        "account": account,
        "exp": datetime.datetime.now() + datetime.timedelta(days=30),
    }
    return encode(payload, secret, algorithm='HS256')


def resolveAccountJwt(token: str) -> dict:
    secret = "this_is_the_secret"
    return decode(token, secret, algorithms='HS256')


if __name__ == '__main__':
    token = makeAccountJwt("gsycl2004")
    print(token)
```

这段代码演示了如何使用 Python 的 PyJWT 库来生成和验证 JWT（JSON Web Token）令牌。

**主要功能**

1. **makeAccountJwt(account: str) -> str**
   - 作用：为指定账户生成 JWT 令牌
   - 参数：account - 要编码到令牌中的账户名（字符串）
   - 返回值：JWT 令牌字符串
   - 实现细节：
     - 使用固定密钥 "this_is_the_secret"
     - 令牌有效载荷(payload)包含：
       - account：账户名
       - exp：过期时间（当前时间 + 30天）

2. **resolveAccountJwt(token: str) -> dict**
   - 作用：解析验证 JWT 令牌
   - 参数：token - 要解析的 JWT 令牌字符串
   - 返回值：解码后的令牌内容（字典形式）
   - 实现细节：
     - 使用相同的固定密钥验证令牌
     - 会自动检查令牌是否过期

**测试部分**

当直接运行这个脚本时：

- 会为账户 "gsycl2004" 生成一个令牌
- 打印出生成的令牌！

所以 login 中通过 jwt 制作了一个 token，并且返回生成的 token。

接下来是注册 register 接口：

用到了 database 中的 save_account 函数，保存账号信息到数据库，返回 token。

### 2.2 info

info 中第一个接口为 user，功能是查询用户是否存在，因为 token 加密的时候将用户名也添加进入用户系统，那我就需要判断，这个 token 解码出来的用户是否存在于我的数据库中，如果用户存在存在返回用户数据，不存在返回用户不存在。

第二节接口 info：这个 FastAPI 路由接口是用来获取用户详细信息的。当客户端发送一个 POST 请求到 `/api/info` 时，服务端会先解析请求中包含的 JWT 令牌来验证用户身份，从中提取出用户名。接着系统会根据这个用户名去查询用户的详细信息，如果查询不到用户数据，就会返回一个错误响应，提示"用户没有设置信息"并附带状态码 33，同时会在响应中带上基本的用户名信息。如果能成功查询到用户数据，就会把这些用户详细信息按照 Form 的数据结构进行封装，然后返回给客户端。整个过程实现了基于令牌认证的用户信息查询功能，既保证了接口的安全性，又能根据用户是否存在详细信息返回不同的响应格式。

第三个接口 submitinfo：当客户端向 `/api/submitInfo` 发送 POST 请求时，服务端首先会从请求中提取 JWT 令牌，通过解析这个令牌获取当前登录的用户名。然后接口会处理请求中的表单数据，特别是将表单中的生日日期（birth 字段）从 Python 的 datetime 对象转换为 ISO 格式的字符串，这样做的目的是为了让数据符合 JSON 的格式要求。接着，接口会将整个表单数据转换为字典格式，并调用保存用户详情的函数，把这些数据与用户名关联存储到数据库中。最后，接口返回一个空的成功响应，表示用户信息已成功提交并保存。整个过程实现了用户信息的验证、格式转换和持久化存储，确保数据能够被正确地保存并在后续查询中使用。

第四个接口上传头像：首先客户端会带着 JWT 令牌和头像图片的 ID 发起 POST 请求。服务端收到请求后，会先创建一个新的数据库会话（session），然后解析 JWT 令牌获取当前登录的用户名。

接着接口会查询该用户是否已有详细信息记录：

- 如果查询不到用户详情（detail 为 None），就会新建一个 UserDetail 对象，并以用户名作为 ID
- 如果已有记录，就直接使用现有记录

然后接口会去图片表中查询请求中指定的图片 ID 对应的图片记录。找到后，将这个图片对象关联到用户的 detail 记录上，具体是通过设置 `detail.image = image` 来实现的。

最后提交事务（`session.commit()`）保存这个关联关系，并返回一个空的成功响应。整个过程确保了用户和头像图片的正确关联，并且如果用户是第一次设置信息，会自动创建基础记录。

第五个是寻找头像，并且返回头像流展示。

### 2.3 report

**submitreport:**

这个 `/api/submitReport` 接口是一个用于提交医疗检查报告的 POST 接口，它会先通过解析请求中的 JWT 令牌来验证用户身份并获取用户名，然后构建一个包含完整报告信息的字典对象，其中包含了提交用户、负责医生、当前日期作为提交时间、初始状态设为"Checking"表示待检查、相关的医学影像数据，以及预留但初始为空的诊断相关字段（包括诊断内容、诊断医生、诊断时间和诊断结果），最后调用 `save_report` 函数将这些结构化数据保存到数据库中，并返回一个空的成功响应表示报告已成功提交。整个流程不仅实现了基本的报告提交功能，还通过精心设计的数据结构为后续的诊断流程预留了完善的状态管理字段，包括自动记录提交时间、初始化报告状态、关联用户和医生信息等，确保了报告数据的完整性和可追溯性。

**getreport:**

这个 `/api/getReports` 接口用于获取医疗检查报告列表，其完整工作流程如下：服务端首先会解析请求中的 JWT 令牌以验证用户身份并提取用户名，然后根据用户名查询账户信息以确定用户类型（如普通用户或医生等不同角色），接着调用 `get_user_reports` 函数获取该用户的原始报告数据（根据用户类型可能返回不同范围的报告），在返回响应前会对每条报告数据进行格式处理，特别是将字符串格式的提交时间（submitTime）转换为日期对象，同时使用异常捕获机制确保单条报告数据处理失败时不会影响整个请求，最后将所有有效报告数据封装成 Report 对象列表并通过 ReportResponse 返回给客户端，整个流程既保证了数据安全性又提供了良好的容错机制，使得前端能够获取到规范化的报告数据。

**reportdelete:**

这个 `/api/report/delete` 接口用于删除医疗检查报告，其完整处理流程如下：首先通过解析请求头中的 JWT 令牌验证用户身份并获取用户名，然后根据请求参数中的报告 ID 加载对应的报告数据。如果找不到指定报告，则立即返回 404 错误响应；找到报告后会进行严格的权限验证，只有当请求用户是该报告的提交用户(user)或负责医生(doctor)时才允许执行删除操作，否则返回 403 未授权错误。通过权限检查后，系统会调用删除功能，若删除成功则返回空响应，若删除过程中出现异常则返回 500 服务器错误。这种设计既确保了数据安全性，通过双重身份验证（用户或医生）来保护报告数据不被未授权删除，又提供了清晰的错误反馈机制，包括报告不存在、无操作权限和服务器错误等不同场景的区分处理。

**reportdetail:**

这个 `/api/report/detail` 接口用于获取医疗报告的详细信息，其完整处理流程如下：首先通过解析请求中的 JWT 令牌验证用户身份并获取用户名，然后根据请求参数中的报告 ID 加载对应的报告数据，如果找不到指定报告则返回 404 错误响应。获取到报告数据后，系统会将字符串格式的提交时间转换为日期对象，同时查询并加载该报告的所有评论数据，将每条评论数据封装成 CommentModel 对象列表。最后将所有报告详情数据和评论列表整合到 ReportDetailResponse 响应对象中返回给客户端。在整个处理过程中，系统通过异常捕获机制确保任何数据处理错误都能被捕获并转换为 500 服务器错误响应，同时保持了清晰的错误信息反馈。这种设计既保证了接口的健壮性，又能提供完整的报告详情和关联评论数据，方便前端展示报告的所有相关信息。

**submit:**

首先通过解析 JWT 令牌获取当前操作医生的用户名，然后加载指定 ID 的检查报告数据，如果报告不存在则返回 404 错误。接着进行严格的权限验证，确保只有该报告指定的负责医生（report_data 中的 doctor 字段）才能提交诊断结果，否则返回 403 未授权错误。通过验证后，系统会更新报告数据，将请求中的诊断内容（diagnose）写入报告，并将报告状态标记为已完成（ReportStatus.Completed）。最后先保存更新后的报告数据再删除原报告（此处 delete_report 的调用可能需要确认其必要性，通常更新后直接保存即可），返回空响应表示操作成功。整个流程特别注重医疗数据的安全性，通过医生身份与报告绑定关系的严格校验，确保只有指定医生才能提交诊断结果，同时完整记录了诊断信息并更新了报告状态，实现了诊断流程的闭环管理。

## 3、api/doctor

其接口大部分与用户接口一样，列出医生列表、修改信息、设置信息、上传信息等。

所以理解了 user 的 api 接口，doctor 对应的接口也是都能理解。
